# 人工消息任务自定义频控实施计划

**Goal:** 支持操作者配置同类消息最大次数和滚动时间窗口。

## 用户指定约束

- 直接修改，不新增或运行自动化测试。
- 本任务仍固定每个 UID 单次发送。

## 实施任务

1. 为人工任务增加 `ManualFrequencyPolicy` 数据结构。
2. 将预设频控下拉替换为“不启用/自定义”。
3. 自定义模式显示次数、窗口值和小时/天单位，并校验正整数。
4. 将频控写入任务草稿、提交对象和预览摘要。
5. 同步消息任务 PRD。
6. 运行生产构建和差异检查，不运行测试套件。
7. 提交实现。
