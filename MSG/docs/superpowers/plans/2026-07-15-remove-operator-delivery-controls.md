# 移除人工任务发送执行控件实施计划

**Goal:** 从人工任务发送策略中移除发送速率和 UID 去重控件。

## 实施任务

1. 删除 `CreateTaskPage` 中发送速率输入框、UID 去重开关和初始值。
2. 删除不再使用的 `Switch` 导入。
3. 更新消息任务 PRD，明确 UID 去重归属目标用户模块、速率归属后台发送服务。
4. 运行聚焦测试、生产构建和差异检查。
5. 提交实现。
